! You have to create the SNAPSHOT's (mvn package -DskipTests) before use docker compose up....

Spring Cloud + OpenFeign -> Used in the operations microservice to establish communication with the vehicles microservice
